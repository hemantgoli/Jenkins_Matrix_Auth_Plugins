pipeline{
	agent any
	stages{
		stage('Git checkout'){
			steps{
				echo "checkout done"
			}
		}
		stage('Build'){
			steps{
			echo ${env.NODE_NAME}
			sh '''cd /opt
			      ls -lrt
			      df -h
			      pwd
			   '''
			}
		}
	}
}
