pipeline{
	agent any
	stages{
		stage('Git checkout'){
			steps{
				echo "checkout done"
			}
		}
		stage('Build'){
			steps{
			sh '''cd /opt
			      ls -lrt
			      df -h
			      pwd
			   '''
			}
		}
	}
}
